<INSTRUCTIONS>
项目条件：
- 项目实现 SpaceX（Falcon 9）式火箭自动定点回收：从任意空中状态开启自动模式后，经滑行 →（需要时）反推返航 → 最优时刻点火的一次连续着陆燃烧直接到触地（2D 与 3D 均不设单独的末端下降阶段），落到发射台中心。
- 制导使用凸优化动力下降规划（Açıkmeşe–Ploen 无损凸化 / G-FOLD 结构）：推力锥 ‖u‖ ≤ Γ、ρ_min ≤ Γ ≤ ρ_max、随剩余时间收紧的推力倾角约束、以台面为顶点的滑翔锥（软约束）、油门与侧向推力变化率约束、预测气动阻力、终端状态用精确 L1 罚函数保证问题恒可行。
- 飞行时间为自由终端时间：点火前以燃料最优搜索决定点火时机；点火后锁定触地时间、以平滑目标重规划；无法按时到达时只能移到“最早可达”时刻，不得单纯延长时间（会导致贴地悬停）。
- 规划时域覆盖“从现在到触地”（N = 40 节点、推力一阶保持），不再使用固定 600 秒时域；绿线显示规划轨迹，蓝线显示实际轨迹。
- 求解器使用 Clarabel：矩阵在 Python（guidance.py）中组装，原生 DLL（native/src/lib.rs，ABI 2，文件名 clarabel_hover.dll）只做通用锥规划求解，Python clarabel 包作为同一问题的后备。修改 lib.rs 后运行 native\build_solver.ps1 重新编译。
- 规划推力上限留出反馈余量（点火规划 80%，着陆燃烧 85%）；内环为规划推力前馈 + PD 修正 + 级联 TVC 姿态环（限幅角速度、±12° 摆角、角速度前馈）；发动机关闭时允许用 RCS 调姿。
- 保留普通地面碰撞和地图边界碰撞；只有落点误差 ≤ 8 m、速度 ≤ 4 m/s、姿态 ≤ 12°、角速度 ≤ 20°/s 的接触才判定为 LANDED。
- 必须用无窗口闭环仿真实测（python main.py --landing-test）：100 m 静止、2000 m 垂直速度 +/-30 m/s、2000 m 水平位置 +/-1000 m 且水平速度 +100 m/s。验收标准：LANDED、落点误差 ≤ 1 m、触地下沉 ≤ 2 m/s、水平速度 ≤ 1 m/s、姿态 ≤ 5°，由 Clarabel 求解。
- python main.py --record <目录> 用游戏自身渲染器离屏录制上述场景的视频（需要 imageio-ffmpeg，缺失时输出 JPEG 帧）。
- 画面：visuals.py 负责箭体、尾焰、烟尘和着陆台；cockpit.py 负责模拟舱风格仪表（PFD、自动驾驶面板、发动机油门滑杆、NAV、最后 50 m 的着陆区精度监视器）。所有仪表只读 Telemetry，不参与控制。
</INSTRUCTIONS>
- 3D 版是独立入口 main3d.py（不得改动 2D 的 main.py/guidance.py 行为）：rocket3d.py 6 自由度 + 推进剂/质量/质心/惯量变化 + TVC/栅格舵/RCS，aero3d.py 工程气动 + ExternalCFD（接收浏览器 GPU 3D CFD 的力系数，按 C 驱动受力，限制在工程模型 0.5–2 倍内，>1 s 无数据退回模型），guidance3d.py 3D G-FOLD（复用 guidance.py 的 Clarabel 后端，气动法向力对推力方向逐次线性化进规划），sim3d.py 场景与 KSP 式手控，web3d/ 为 WebGL2 前端（fluid.js 是 3D GPU 流体；前端只读状态，经 /api/input 发按键，经 /api/cfd3d 发 CFD 力系数）。
- 3D 自动驾驶结构（2026-09 重写）：反推返航、滑翔、着陆燃烧三者共用进场预测器 `_predict_approach`（滑行 + 对数风切变 + 用真正的着陆燃烧控制律飞到 200 m），目标一致：燃烧到 200 m 时在台面正上方且水平速度≈0，点火时水平速度≈0（侧风时点火点在上风处）。反推（`_boostback_dv`）对预测器求 2×2 有限差分灵敏度、加权最小二乘求 Δv，剩余 < 1 m/s 结束，动压 > 1 kPa 时油门不低于 30%；滑翔（`_glide_command`）用 ≤15° 攻角升力，a(t)=c0+c1·t/T 由灵敏度加权最小二乘求解，点火前 4–8 s 渐变到 0 让点火时箭体与来流对齐；点火只按竖直需求（与预测器同一判据）。着陆燃烧（`_egd_core`，纯函数，预测器与飞行共用）：竖直跟踪点火时固定的下沉剖面（匀减速段 + 末段 1.2 + 0.4·h，前馈 + 比例反馈）；横向 200 m 以上视线律（水平速度 ∝ 下沉速度，指向 200/150/100/60/30 m 的台面上方目标点，只降不升），以下 ZEM/ZEV 到 4 m 再阻尼，近地侧向速度包络 1.0 + 0.25·h；推力以相对气流逆速度为基准，侧向用 k = 推力加速度 − 箭体升力梯度换算倾角，禁止把测得的侧向气动力反馈回推力指令。绿线由同一控制律正向仿真（`_path_points`）。制导 CPU 必须保持在实时预算内（滑行/反推每仿真秒 < 0.3 s，单步 p99 < 40 ms）：预测器滑行步长 0.5 s（12 km 以上 2 s）、燃烧步长 0.5 s，滑翔/反推 0.5 s 重解、绿线 1 s 刷新且错开。验收用 `metrics.py`（反推结束落点偏差、点火时水平偏差/速度、200 m 处偏差/水平速度、最大倾角、200 m 以下倾角、触地误差/速度），需跑标称 + 气动 0.8/1.25 + 12 m/s 风，全部 LANDED。禁止任何末段悬停/减速等待修正（下沉速度只能单调减小）。旧 G-FOLD 着陆燃烧与 `guidance3d_legacy.py` 保留为可切换后备（`egd_all=False`）。
- 3D 必须用 python main3d.py --test 实测 6 个场景：LANDED、落点 ≤ 3.5 m、下沉 ≤ 2.5 m/s、水平速度 ≤ 1.5 m/s、姿态 ≤ 5°，并检查没有悬停（着陆燃烧中离地 > 1.5 m 时下沉速度不得小于 0.8 m/s）和没有接地前摇摆（离地 < 8 m 时俯仰/偏航角速度 ≤ 12°/s，触地姿态 ≤ 1.5°）。姿态环的角速度指令必须受执行机构可用角加速度限制，推力方向指令有转动速率上限。前端改动后用无头 Chromium（WebGL2/SwiftShader，?cfd=low）截图检查；服务端 JSON 不得含 NaN（_finite 过滤），数值发散时判 NUMERICAL FAULT。
- 前端：相机不得自动旋转（只随鼠标）；仪表面板可拖动并记住位置（L 复位）；姿态球为阿波罗 FDAI 风格、着陆区为带软键的 MFD 式 ND（web3d/instruments.js）；着陆成功不弹 LANDED 横幅；RCS 为白色冷气喷流。
- 网页版：web3d/sim_worker.js + sim_worker.py 在浏览器 Web Worker 里用 Pyodide 运行同一套 3D Python 仿真（无 /api 后端时自动启用，?local 强制）；deploy/build_web.py 生成纯静态包 dist/web3d/，deploy/nginx-rockt3d.conf 静态托管。修改 3D Python 代码或 web3d/ 后需重新构建并同步到 /var/www/rockt3d。
